/// Raios do tema PrimeNG usado no site: campos/menus ~6px, cards/modais
/// ~12px. Bem menores que o protótipo `vibrant_celebration` (que usava
/// raios grandes, "pill"-like) — seguimos o site real.
abstract final class AppRadius {
  static const sm = 4.0;
  static const md = 6.0;
  static const lg = 12.0;
  static const pill = 999.0;
}
