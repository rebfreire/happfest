/// Fonte da marca no site em produção é `Quicksand` (aplicada até no
/// `body`) — usar como fonte única do app, ao invés do `Plus Jakarta Sans`
/// do protótipo `vibrant_celebration`.
abstract final class AppTypography {
  static const fontFamily = 'Quicksand';
}
